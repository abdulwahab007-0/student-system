node_modules/
dist/
database.sqlite
database.sqlite-*
*.log
.DS_Store

# Local dev certificates (mkcert)
.certs/

# Environment variables (secrets) — never commit
.env
.env.local
.env.*.local

# Locally uploaded card photos (persistent only in dev)
uploads/

.vercel
.env*
*.db
better-sqlite3
tmp-*.mjs
